[package]
name = "shell-executor"
version = "0.4.0"
edition = "2021"

[[bin]]
name = "x"
path = "src/bin/x.rs"

[dependencies]
chrono = "0.4"
clap = { version = "4", features = ["derive"] }
portable-pty = "0.9"
crossterm = "0.28"

[target.'cfg(unix)'.dependencies]
signal-hook = "0.3"
